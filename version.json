{
  "version": "1.01",
  "changelog": [
    "[+] Added pre-ban validation using `CheckBan()` to prevent duplicate bans",
    "[+] Wrapped database insert operations in `pcall` for safer error handling",
    "[*] Adjusted flag timing — detection flag now set after successful DB insert",
    "[*] Improved error logging during ban operations",
    "[+] Added unique constraint on key identifiers in database to prevent duplicate records",
    "[*] Optimized detection flow for more consistent ban application"
  ]
}
